# EC2

## Things I always forget

Public IPv4 changes after stopping instance.

Elastic IP is permanent.

SSH

chmod 400 key.pem

ssh -i key.pem ec2-user@IP

Apache

sudo dnf install httpd -y

sudo systemctl enable httpd

sudo systemctl start httpd

Common mistakes

❌ Forgot Security Group

❌ Forgot Route Table

❌ Forgot Internet Gateway

❌ Wrong subnet
